function isEven(num) {
        if (typeof num  === 'number') {
	        var x = Boolean;
	        if (num % 2 == 0) {
		     x = true;
	        } else {
		     x = false;
            }
           console.log(x);
         }else {
            throw new Error ( 'Error: parameter type is not a Number' )
         }
        }

isEven(0);
